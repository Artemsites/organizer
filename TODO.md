**LLM сессии**
> agy(pro): organizer
> cla(pro): organizer
  > agy(mid): 2:organizer
  > gro(mid): organizer

---



**Что сейчас важно мне подучить/вспомнить/попрактиковать:**

* Уверенная работа с современным CSS: логические свойства, clip-path
* Опыт проектирования сложной SCSS-архитектуры;
* Глубокое знание SCSS: миксины с аргументами и content-блоками, функции, @use / @forward и модульная организация, работа с map-ами.
* Понимание принципов анимации (тайминги, easing) и умение их реализовать: CSS-переходы, @keyframes, анимация через transform / opacity без просадок производительности.
* Опыт проектирования собственной библиотеки UI-компонентов без фреймворков - с продуманным API, конфигурируемостью и документацией;

* Уверенное знание DOM API, Intersection Observer / Resize Observer, понимание reflow/repaint и производительности анимации.
* Умение писать UI-модули без фреймворков: изоляция состояния, настраиваемые селекторы, делегирование событий, отсутствие утечек памяти.
* Опыт работы с GSAP: таймлайны, Scroll Trigger, стаггеры;
* Оптимизация Core Web Vitals;

* Уверенный ES6+: модули, деструктуризация, замыкания, асинхронное программирование;
* Уверенное знание TypeScript (типы, интерфейсы, дженерики, типизация DOM) - мы переводим на него кодовую базу.

### С чего начнём практику
Выбирай направление для первого микро-шага:

1. Вариант 1 (SCSS & Modern CSS): Проектируем новую архитектуру стилей
web/src/styles/ (@use/@forward, $breakpoints map, миксин respond-to с @content,
токены в CSS custom properties, логические свойства и clip-path).
2. Вариант 2 (UI-компоненты на Vanilla TS): Начинаем строить фундамент UI-
библиотеки — создаём базовый класс/интерфейс компонента с жизненным циклом
(init/destroy), типами и первой реализацией (Button + Toast с анимацией на
transform/opacity).
3. Вариант 3 (Event Loop & DOM Performance на практике): Разбираем текущий
модуль TodosModule — переводим рендеринг на requestAnimationFrame/Event
Delegation, устраняем потенциальный Layout Thrashing и наглядно препарируем
работу Event Loop в Chrome.

### 1. Трек: SCSS-архитектура & Modern CSS (Шаг 13)
### 2. Трек: Vanilla TS UI-библиотека без фреймворков (Шаг 14)
### 3. Трек: DOM API, Наблюдатели & Event Loop (Шаг 15)
### 4. Трек: GSAP & Core Web Vitals (Шаг 15 + перформанс)
---



* Опыт настройки CI/CD (GitLab CI): сборка, линтеры, автодеплой.

* Опыт работы со сборщиками (Webpack, Vite): понимание пайплайна, умение поправить или дописать конфиг.

* Опыт работы с Hugo или другими генераторами статических сайтов (или Astro);



* Попрактиковать Понимание system design?



**Вопросы на обсуждение**
* Стоит ли в этот проект внедрить redis? чтобы попрактиковаться
* Можем ли мы попрактиковать React/Next, Zustand (Redux Toolkit / React Query / TanStack Query) на этом проекте? Где он был бы кстати? Или здесь не желательно? Вообще у меня есть еще проект где есть next/react & state.